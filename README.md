# openrouter-free-cli-agent
A Python-based CLI agent for openrouter/free (dynamic in-runtime selection of free AI models). This agent features built-in tool support for processing local images as input (via base64 encoding), reading/writing files, and executing bash commands.
